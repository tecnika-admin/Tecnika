<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_move_form_inherit_cogs_adjustment" model="ir.ui.view">
            <field name="name">account.move.form.inherit.cogs.adjustment</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <button name="action_open_cogs_adjustment_wizard"
                            string="Ajustar Costo de Venta"
                            type="object"
                            groups="custom_cogs_adjustment.group_cogs_adjustment_user"
                            invisible="state != 'posted' or move_type not in ('out_invoice', 'out_refund', 'in_invoice', 'in_refund')"
                            help="Abre el asistente para corregir el costo de venta y la valoración de inventario asociados a esta factura."/>
                            
                            </xpath>
            </field>
        </record>
    </data>
</odoo>